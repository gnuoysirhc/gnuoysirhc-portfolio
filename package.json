{
  "name": "gnuoysirhc-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 43123",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 43123",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "wrangler": "^4.131.1"
  }
}
